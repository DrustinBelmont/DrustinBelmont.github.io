import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://drustinbelmont.github.io', // ton URL finale
  outDir: '../dist' // (optionnel : tu peux générer dans ../dist pour déploiement direct)
});
